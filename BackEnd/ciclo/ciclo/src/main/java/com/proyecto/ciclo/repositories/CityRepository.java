package com.proyecto.ciclo.repositories;

import com.proyecto.ciclo.entities.CityEntity;

public interface CityRepository extends BaseRepository<CityEntity, Long> {
    // Add any additional custom methods specific to CityRepository
}
